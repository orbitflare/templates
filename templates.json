[
  {
    "name": "solana-copy-trader",
    "description": "Mirror trades from any wallet",
    "lang": "rust",
    "level": "beginner",
    "tags": ["trading", "copy-trade", "jetstream"],
    "repo": "https://github.com/orbitflare/templates/tree/main/solana-copy-trader",
    "setup_steps": [
      "cp .env.example .env",
      "cp config.example.yml config.yml",
      "# Edit .env with your OrbitFlare gRPC endpoint, RPC URL, and keypair path",
      "docker compose up -d",
      "# Or run locally: docker compose up -d redis postgres && cargo build --release"
    ]
  },
  {
    "name": "solana-blinks-axum",
    "description": "Solana Actions (Blinks) server + frontend",
    "lang": "rust+ts",
    "level": "beginner",
    "tags": ["blinks", "actions", "axum"],
    "repo": "https://github.com/orbitflare/templates/tree/main/solana-blinks-axum",
    "setup_steps": [
      "cp .env.example .env",
      "# Edit .env with your RPC_URL (defaults to mainnet)",
      "cargo run"
    ]
  },
  {
    "name": "orbit-grpc-indexer",
    "description": "Dual-stream Solana transaction indexer with Jetstream + Yellowstone",
    "lang": "rust+ts",
    "level": "intermediate",
    "tags": ["indexer", "grpc", "jetstream", "yellowstone", "postgres"],
    "repo": "https://github.com/orbitflare/templates/tree/main/orbit-grpc-indexer",
    "setup_steps": [
      "cp .env.example .env",
      "# Edit .env with your Jetstream, Yellowstone, and Postgres URLs",
      "docker compose up -d",
      "cargo run --bin orbit-grpc-indexer",
      "# In a separate terminal:",
      "cargo run --bin indexer-api-server",
      "# Optional frontend:",
      "cd frontend && npm install && npm run dev"
    ]
  }
]
